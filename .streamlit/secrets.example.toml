# Example Secrets file for Streamlit Community Cloud deployment.
# Copy this file to `.streamlit/secrets.toml` and fill in your actual keys.
# Do NOT commit the real `secrets.toml` file to version control.

# The Gemini API key used by the O.D.I.N. cognitive engine
GEMINI_API_KEY = "your-google-gemini-api-key-here"
